'use strict';

var userState = {
	gravatar: 'string',
	settings: {
		enabled: true,
		level: 'high',
		duration: 5
	}
}

module.exports = {
	getNewState: function(userId){

	},
	getExistingState: function(userId){
		
	}
}